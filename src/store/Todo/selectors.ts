// type Todo = {
//   id: string;
//   title: string;
//   completed: boolean;
// };
// export const todoComplete = (state) =>
//   state.todos.list.filter((todo<Todo>) => todo.complete === true);

export const todoComplete = [];
